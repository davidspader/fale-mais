<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/style.css" rel="stylesheet" type="text/css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Righteous&display=swap" rel="stylesheet">
    <title>Fale Mais</title>
</head>

<body>
    <div class="container">
        <div class="card p-4 mt-5">
            <h1 class="titulo m-5">FALE MAIS</h1>
            <form method="post" action="/">
                <div class="row">

                    <div class="col-md-4">
                        <label for="idValor" class="form-label">Códigos das cidades</label>
                        <select name="idValor" id="idValor" class="form-select">
                        <% for(let i = 0; i < faleMais.valores.length; i++) {
                            let selected = ( body.idValor == i ) ? "selected" : "";
                        %>
                            <option value="<%=i%>" <%=selected%>>De: <%=faleMais.valores[i][0]%> Para: <%=faleMais.valores[i][1]%></option>
                        <%}%>
                        </select>
                    </div>

                    <div class="col-md-4">
                        <label for="tempoLigacao" class="form-label">Tempo de ligação</label>
                        <input class="form-control" type="number" name="tempoLigacao" id="tempoLigacao" value="<%=body.tempoLigacao%>">
                    </div>

                    <div class="col-md-4">
                        <label for="idPlano" class="form-label">Plano</label>
                        <select name="idPlano" id="idPlano" class="form-select">
                        <% for(let i = 0; i < faleMais.planos.length; i++) {
                            let selected = ( body.idPlano == i ) ? "selected" : "";
                        %>
                            <option value="<%=i%>"<%=selected%>>Fale Mais <%=faleMais.planos[i]%></option>
                            
                        <%}%>
                        </select>
                    </div>

                </div>
                <div class="row mt-2">
                    <div class="col-md-4 d-grid button-div">
                        <button class="btn" type="submit">Calcular</button>
                    </div>
                </div>
            </form>
            <% if(resultado){%>
                <div class="resultado text-center m-3">
                    <h2>
                        Você pagará: R$
                        <%=resultado.comPlano.toFixed(2)%>
                    </h2>
                    <span>
                        Sem o plano você pagará: R$
                        <%=resultado.semPlano.toFixed(2)%>
                    </span>
                </div>
                <%}%>
        </div>
    </div>
</body>

</html>